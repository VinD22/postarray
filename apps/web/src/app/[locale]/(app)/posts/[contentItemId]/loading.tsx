import type { ReactElement } from 'react';

import { ReceiptRouteFallback } from '@/features/receipts/receipt-fallback';

export default function PostLoading(): ReactElement {
  return <ReceiptRouteFallback />;
}
