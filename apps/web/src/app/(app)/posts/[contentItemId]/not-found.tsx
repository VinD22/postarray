import type { ReactElement } from 'react';

import { ReceiptNotFound } from '@/features/receipts/receipt-fallback';

export default function PostNotFound(): ReactElement {
  return <ReceiptNotFound />;
}
